catalog:
  name: iceberg

datasets:
  # S3 bucket name matches --env (local, homelab, aws). Endpoint/credentials in .env.{env}.
  warehouse: "s3a://local"

spark:
  master: "local[*]"
  configs:
    spark.sql.shuffle.partitions: "4"
    spark.hadoop.fs.s3a.endpoint: "${S3A_ENDPOINT}"
    spark.hadoop.fs.s3a.access.key: "${S3A_ACCESS_KEY}"
    spark.hadoop.fs.s3a.secret.key: "${S3A_SECRET_KEY}"
    spark.hadoop.fs.s3a.path.style.access: "true"
    spark.hadoop.fs.s3a.impl: "org.apache.hadoop.fs.s3a.S3AFileSystem"
    spark.hadoop.fs.s3a.connection.ssl.enabled: "false"
    spark.hadoop.fs.s3a.aws.credentials.provider: "org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider"
    # Match PySpark 4.x bundled Hadoop (3.4). hadoop-aws 3.3.x breaks s3a with NumberFormatException "60s".
    spark.jars.packages: "org.apache.hadoop:hadoop-aws:3.4.1,com.amazonaws:aws-java-sdk-bundle:1.12.367"
