spark:
  configs:
    spark.sql.shuffle.partitions: "1"

datasets:
  input:
    orders:
      type: table
      table: raw.orders
    impression:
      type: table
      path: landing/impression/ymd={ymd}
      format: parquet
  output:
    main:
      type: path
      path: mart/hello_world/ymd={ymd}
      format: parquet
      mode: overwrite
